# 第一篇上手说明

如果以后想继续写内容，最简单的方式就是：

1. 新建一个 Markdown 文件
2. 在侧边栏配置里加进去
3. 提交到仓库，让 Pages 自动部署

## 本地预览

```bash
npm install
npm run dev
```

## 构建静态文件

```bash
npm run build
```

构建完成后，生成的内容会在：

```text
docs/.vuepress/dist
```

## 推荐写法

- 一页一个主题
- 标题别太花，靠结构清楚取胜
- 能贴命令就贴命令，能举例就举例
